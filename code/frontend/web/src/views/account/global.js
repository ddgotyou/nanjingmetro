const defaultStudentUserGroupId = 999;
const defaultTeacherUserGroupId = 999;
const inputWidth = 375;

export default {
  defaultStudentUserGroupId, // 默认学员用户组 ID
  defaultTeacherUserGroupId, // 默认教师用户组 ID
  inputWidth, // 输入框宽度
}